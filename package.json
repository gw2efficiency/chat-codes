{
  "name": "gw2e-chat-codes",
  "version": "1.3.1",
  "description": "Encode and decode Guild Wars 2 chat codes",
  "main": "./build/src/index.js",
  "scripts": {
    "build": "rm -rf build/ && tsc",
    "test": "jest",
    "format": "prettier --write 'src/**/*.ts' 'tests/**/*.ts'",
    "lint": "prettier --check 'src/**/*.ts' 'tests/**/*.ts'",
    "version": "npm run build"
  },
  "author": "queicherius@gmail.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/gw2efficiency/chat-codes"
  },
  "dependencies": {
    "base64-js": "^1.1.2"
  },
  "devDependencies": {
    "@types/base64-js": "^1.2.5",
    "@types/jest": "^24.9.0",
    "jest": "^24.9.0",
    "prettier": "^2.0.2",
    "ts-jest": "^24.3.0",
    "typescript": "^3.7.5"
  }
}
